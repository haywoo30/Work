---
title: "STT180_Day13"
author: "Jacob Haywood"
date: "2/19/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
paretoCDF <- function(alpha = 2, x0 = 1, x = 1){
  cdf = rep(0, length(x))
  cdf[x > x0] <- 1 - (x0/x[x > x0])^(alpha - 1)
  return(cdf)
}

paretoQuantile <- function(alpha, x0, p,lower.tail=TRUE) {
  stopifnot(0 <= p, p <= 1, alpha > 1, x0 >= 1)
  if (lower.tail == FALSE){
    p <- 1-p
  }
q <- x0*((1-p)^(-1/(alpha-1)))
return(q)
}

paretoQuantile(p=0.98,alpha=3,x0=5,lower.tail=FALSE)

paretoQuantile(p=0.08,alpha=2.5,x0=1e6,lower.tail=FALSE)
```


```{r}
paretoQuantile(p=0.08, alpha = 2.5, x0 = 1e6, lower.tail=FALSE)
```

